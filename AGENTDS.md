1. review 需要从正确性，性能，结构，边界，复杂，代码干净，设计，以及测试是否充分到位
2. 现在的工程没有历史数据没有历史负担，不需要兼容过时的设计，保持一版新设计。当我需要兼容的时候，我会告诉你。
3. 不要自作聪明，我让你改什么就改什么，不要随意扩大边界，去改设计。如果确实遇到设计不合理，要告知我，我review之后，再去修改。
4. 把过时废弃的代码就地删掉。
5. 单元测试代码和生产代码放在不同文件。需要访问私有实现的单元测试按生产目录集中放在该目录的 `tests/` 下，测试文件与生产文件同名，并由 `tests/mod.rs` 统一声明；只测试公开 API 的集成测试放在 `kv_engine/tests`。
6. mod.rs 不要放功能实现得代码，保持模块声明即可。测试相关的抽取出来，按照第5条规则放入相应的测试文件。
7. 不要为了benchmark的数据好看，而做很飞线。